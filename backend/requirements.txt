# ═══════════════════════════════════════════════════════════════
# AGRISENSE — Backend Dependencies
# ═══════════════════════════════════════════════════════════════

# ── Core Web Framework ──
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
pydantic-settings==2.4.0
python-dotenv==1.0.1
python-multipart==0.0.9

# ── Database ──
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0

# ── Auth / Security ──
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ── Image Processing (Disease Detection) ──
Pillow==10.4.0
opencv-python-headless==4.10.0.84
numpy==1.26.4

# ── ML / AI ──
# torch + torchvision installed separately
# ONNX Runtime for model inference
onnxruntime==1.19.2

# ── Weather API ──
httpx==0.27.2

# ── Utilities ──
structlog==24.4.0
aiofiles==24.1.0

# ── Testing ──
pytest==8.3.4
pytest-asyncio==0.25.0
